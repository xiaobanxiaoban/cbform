第一阶段（2013-1-27到2013-2-28）
基本功能点：
1、表单配置，表单主要支持以下几种类型：
  文本（text包含：密码，日期，普通）
  多行文本（textarea）
  单选（select 级联）
  多选（select multi 级联）
  单选组（radio）
  多选组（checkbox）
  富文本编辑器（uedit）
  文件上传（file）
  超链接(<a></a>)/按钮（<button></button>）弹出选择框
  多表格（多表格嵌套）